{
  "name": "hall-confirmation-contract",
  "version": "0.1.0",
  "private": true,
  "description": "Solidity confirmation-ledger contract for the UI Alumni Halls Fund. Self-contained Hardhat project — deliberately kept separate from the Next.js app's package.json/tsconfig so contract tooling never affects the web build.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:amoy": "hardhat run scripts/deploy.ts --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
