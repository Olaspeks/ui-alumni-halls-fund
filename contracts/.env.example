# Used by hardhat.config.ts when deploying. These are the SAME two
# values you'll copy into the main app's .env.local as CHAIN_RPC_URL /
# CHAIN_PRIVATE_KEY once deployed — see ../contracts/README.md.

CHAIN_RPC_URL=https://rpc-amoy.polygon.technology
CHAIN_PRIVATE_KEY=
